<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="16dp"
    android:paddingTop="16dp"
    android:paddingRight="16dp"
    android:paddingBottom="16dp"
    android:background="@drawable/fondo"
    tools:context="com.example.appvicentemiscocos.AdminActivity">

    <Button
        android:id="@+id/userListButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Listar Usuarios"
        android:fontFamily="sans-serif-black"
        android:textColor="#CFCFCF"
        android:layout_marginTop="16dp"
        android:background="@drawable/button_background" />

    <Button
        android:id="@+id/changePasswordButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Cambiar Contraseña"
        android:layout_below="@id/userListButton"
        android:fontFamily="sans-serif-black"
        android:textColor="#CFCFCF"
        android:layout_marginTop="16dp"
        android:background="@drawable/button_background" />

    <Button
        android:id="@+id/deleteAllUsersButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Borrar Todos los Usuarios"
        android:layout_below="@id/changePasswordButton"
        android:fontFamily="sans-serif-black"
        android:textColor="#CFCFCF"
        android:layout_marginTop="16dp"
        android:background="@drawable/button_background" />

    <Button
        android:id="@+id/backButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Volver"
        android:layout_below="@id/deleteAllUsersButton"
        android:fontFamily="sans-serif-black"
        android:textColor="#CFCFCF"
        android:layout_marginTop="16dp"
        android:background="@drawable/button_background" />

</RelativeLayout>
